import { integrationGradient } from "../components/radial-gradient/utils";

const NEOVIM_GRADIENT = integrationGradient([94, 255, 160]);
const WEZTERM_GRADIENT = integrationGradient([78, 73, 238]);
const HYPRLAND_GRADIENT = integrationGradient([0, 175, 240]);
const SPOTIFY_GRADIENT = integrationGradient([30, 215, 96]);
const DISCORD_GRADIENT = integrationGradient([88, 101, 242]);

export {
  NEOVIM_GRADIENT,
  WEZTERM_GRADIENT,
  HYPRLAND_GRADIENT,
  SPOTIFY_GRADIENT,
  DISCORD_GRADIENT
}
